<!DOCTYPE html>
<html xml:lang="{{applocale}}" lang="{{applocale}}" isolang="{{isolang}}" dir="{{lang.applangdir}}">
    <head data-headertemplate="{{headertemplate}}">
		<link rel="stylesheet" href="{{subfolder}}/js/jstree/themes/default/style.css" />
        {% include 'components/common_head.twig' %}

		<script src="{{subfolder}}/js/jstree/jstree.min.js"></script>
		<style type="text/css">
		.webfx-tree-children {
		    background-image: url("{{icons.I}}");
		}
		.refreshTree {
			position: absolute;
			right: 0;
			z-index: 1000;
		}
		</style>
    </head>